// Tuple in ts

// Tuple is a typed array with pre-defined type and length of each index

// for example
let person: [string, number, boolean]; //define tuple

person = ["Joy", 10, true]; //create an array with defined tuple

console.log(person);
